# Installation
```
git clone [repo] .dotfiles

git submodule init
git submodule update

# tools (Ubuntu)
./install-ubuntu.sh

# tools (macOS / Homebrew)
./install-homebrew.sh

./install-ohmyzsh.sh
./install-astronvim.sh
./copyfiles.sh
```
